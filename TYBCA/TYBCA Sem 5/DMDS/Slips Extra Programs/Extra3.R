emp_data <- data.frame(
  EmpName = c("Amit", "Ravi", "Priya", "Neha", "Arjun"),
  Gender = c("M", "M", "F", "F", "M"),
  Age = c(28, 34, 25, 29, 40),
  Designation = c("Manager", "Analyst", "Clerk", "HR", "Developer")
)

print(emp_data)

summary(emp_data)

