package main
import "fmt"

func swap(x *int, y *int) {

	var temp int
	
	temp = *x
	*x = *y
	*y = temp
}

func main(){
	var a, b int = 10, 20
	
	fmt.Printf("Before Swap: a: %v b: %v", a, b)
	swap(&a, &b)
	fmt.Printf("After Swap: a: %v b: %v", a, b)
}
