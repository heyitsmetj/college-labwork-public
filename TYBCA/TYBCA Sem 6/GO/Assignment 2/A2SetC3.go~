package main
import "fmt"

func nameret(n int) (x, y int){
	x = n + 5
	y = n - 5
	return
}

func main(){
	var x, y int = nameret(15)
	fmt.Printf("Multiple values returned from a functionn + 5: %v, n - 5: %v\n", x, y)
}
