package main

import "fmt"

func main() {
	numbers := []int{
		10,
		20,
		30,
		40,
		50,
	}

	fmt.Println("Elements of the slice:")
	for i, value := range numbers {
		fmt.Printf("Index %d: %d\n", i, value)
	}
}

